<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">

        <!-- <record id="ir_cron_run_reset_sequence" model="ir.cron">
            <field name="name">Reset Sequence</field>
            <field name="model_id" ref="model_mrp_production"/>
            <field name="state">code</field>
            <field name="code">model._do_reset_sequence()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="nextcall" eval="(DateTime.now().replace(hour=0, minute=0, second=0, microsecond=0, day=1) + timedelta(months=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="numbercall">-1</field>
            <field eval="False" name="doall"/>
        </record> -->
        
    </data>
</odoo>
